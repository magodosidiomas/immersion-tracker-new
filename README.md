# Imerso

Track immersion and study time for language learning. Add multiple languages, one active at a time.

## Stack

- React + Vite
- IndexedDB for local persistence (no backend for MVP, data layer kept abstracted for an eventual API swap)

## Run locally

```
npm install
npm run dev
```
